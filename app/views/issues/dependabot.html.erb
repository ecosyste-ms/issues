<% @meta_title = "Dependabot pull requests" %>

<div class="container-sm">
  <h1 class='mb-3'>
    <%= link_to @host, host_path(@host) %> /
      
    Dependabot issues and pull requests
  </h1>
  
  <%= render @issues %>
  <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>  
</div>